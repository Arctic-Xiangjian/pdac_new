# Ignore everything by default.
*

# Allow Git to traverse directories so nested source files can be included.
!*/

# Keep repository control files.
!.gitignore

# Keep source code and runnable scripts.
!*.py
!*.sh

# Keep minimal project metadata needed to recreate the environment.
!pyproject.toml
!uv.lock

!*.yaml
